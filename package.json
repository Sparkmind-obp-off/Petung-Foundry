{
  "name": "petung-foundry",
  "version": "1.0.0",
  "description": "Pétung — Outcome Foundry untuk hajat & keputusan bermakna khas Jawa (OaaS). by SparkMind.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "dev:sandbox": "wrangler pages dev dist --ip 0.0.0.0 --port 3000",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "npm run build && wrangler pages deploy dist",
    "deploy:prod": "npm run build && wrangler pages deploy dist --project-name petung-foundry",
    "test": "node --test test/",
    "clean-port": "fuser -k 3000/tcp 2>/dev/null || true",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings"
  },
  "dependencies": {
    "hono": "^4.6.14"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250705.0",
    "@hono/vite-cloudflare-pages": "^0.4.2",
    "vite": "^5.4.11",
    "wrangler": "^3.99.0",
    "typescript": "^5.7.2"
  }
}
